'use strict'
const IMG_KEY = 'imgs';
window.onload=_createImage()
function _createImage() { 
var gImg = getFromStorage(IMG_KEY)
if (!img || !img.length) {
    img=[]
}
var gImgs = [{ id: 1, url: 'img/popo.jpg', keywords: ['happy'] }];
}